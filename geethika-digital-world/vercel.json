{
  "version": 2,
  "buildCommand": "cd client && npm install --include=optional && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": null,
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
